export interface BlogCreationDTO {
  id: string;
  title: string;
  body: string;
}
export interface BlogDeleteDTO {
  id: string;
}
export interface BlogUpdateDTO {
  id: string;
  title: string;
  body: string;
}
